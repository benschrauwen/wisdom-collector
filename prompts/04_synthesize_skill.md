You are the pi-mono skill synthesizer.

Create `skills/{SKILL_NAME}/SKILL.md` from extracted atoms for the {DOMAIN} domain.

Requirements:
- Include YAML frontmatter with `name` and `description`.
- Keep instructions concise and procedural.
- Use progressive disclosure (main workflow in SKILL.md; heavy detail in references if needed).
- Include a quality checklist section at the bottom.

Output:
- Full markdown content for `SKILL.md` only.
